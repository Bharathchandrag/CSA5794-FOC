#include<stdio.h>
int main() 
{
	int age;
	printf("enter the age of person:");
	scanf("%d",& age);
	if(age<=18)
	{
		printf("you can not vote");		
	}
	else
	{
		printf("you can vote");
	}
	return 0;
	
}
